# Two jobs on one GPU: the second waits until the first finishes.
python -c "import time; print('job 1 start'); time.sleep(5); print('job 1 done')"
python -c "import time; print('job 2 start'); time.sleep(2); print('job 2 done')"
